% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/execution.R
\name{solve.gams_problem}
\alias{solve.gams_problem}
\title{Solve a GAMSr problem with a local GAMS installation}
\usage{
\method{solve}{gams_problem}(
  a,
  b,
  ...,
  work_dir = NULL,
  system_directory = NULL,
  solver = NULL,
  gams_options = list(),
  solver_options = NULL,
  data = c("gdx", "inline"),
  keep = FALSE,
  timeout = Inf,
  echo = FALSE
)
}
\arguments{
\item{a}{A problem created by \code{\link[=gams_problem]{gams_problem()}}.}

\item{b}{Unused; present for compatibility with the base \code{solve()} generic.}

\item{...}{Reserved for future options.}

\item{work_dir}{Optional work directory. When \code{NULL}, a temporary directory is
created.}

\item{system_directory}{Optional GAMS system directory.}

\item{solver}{Optional solver name. When supplied, GAMSr passes a safe GAMS
command-line solver override for the problem type, such as \code{lp=soplex}.}

\item{gams_options}{Named list of additional scalar GAMS command-line
options, such as \code{list(reslim = 60, optcr = 0.01)}.}

\item{solver_options}{Optional named list of solver-specific option-file
entries. Requires \code{solver}; GAMSr writes \verb{<solver>.opt} in the work
directory and passes \code{optfile=1} to GAMS.}

\item{data}{How input set and parameter records are supplied to GAMS.
\code{"gdx"} writes and loads \code{input.gdx}; \code{"inline"} renders records directly
into the generated \code{.gms} source.}

\item{keep}{Whether to retain work files after reading results.}

\item{timeout}{Maximum process runtime in seconds.}

\item{echo}{Whether to echo GAMS output while it runs.}
}
\value{
A \code{gams_result} object.
}
\description{
This method writes deterministic GAMS source, runs a local GAMS executable
without invoking a shell, reads the result GDX, and returns tidy R records.
}
